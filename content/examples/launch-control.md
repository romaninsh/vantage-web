+++
title = "Launch Control"
description = "A live mission-control console for global spaceflight — 15 related entities, real-time launch countdowns and a deliberately flaky API the UI rides straight through. The whole app is a folder of YAML talking to a real Rust server."
template = "launch-control.html"
+++
