# Vantage 0.6

A release for working with tabular data. Click a column header to sort and the choice is remembered
per project, so reopening a grid picks up where you left off. Long-running grid views keep
themselves current without a manual reload.

## What's new

- Open a single row in a side sheet for a scrollable view of every field, instead of widening
  columns and scrolling sideways.
